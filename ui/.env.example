# OurBlock UI Environment Variables

# Holochain Connection
VITE_HC_HOST=localhost
VITE_HC_PORT=8888
VITE_HC_ADMIN_PORT=37397

# App Version (for update checking)
VITE_APP_VERSION=0.1.0

# Holochain Bootstrap Server (for peer discovery and NAT traversal)
VITE_BOOTSTRAP_SERVER_URL=https://bootstrap.holochain.org

# Optional: Custom mDNS hostname for local discovery (default: ourblock.local)
# VITE_MDNS_HOSTNAME=ourblock.local
